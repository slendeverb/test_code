package SimpleOS.Simulation;

import java.util.ArrayList;
import java.util.List;
import java.util.PriorityQueue;

import SimpleOS.Simulation.Sync.Monitor;

public class OS {
	
	private OS() {}
	
	public static OS getInstance() {
		return Singleton.INSTANCE.getInstance();
	}
	
	private enum Singleton{
		INSTANCE;
		private OS instance;
		Singleton() {
			instance=new OS();
		}
		public OS getInstance() {
			return instance;
		}
	}
	
	public String run() {
		return "Operating System Running...";
	}
	
	public String stop() {
		readyQueue.clear();
		blockQueue.clear();
		return "Operating System Stopping...";
	}
	
	public void block(Process process,List<Process> blockList) {
		process.block(blockList);
	}
	
	public void wakeup(List<Process> blockList) {
		
	}

	private long systemTime=0;
	private Monitor monitor=new Monitor();
	private ArrayList<Process> blockQueue=new ArrayList<Process>();
	private ArrayList<Process> readyQueue=new ArrayList<Process>();
	private ArrayList<Process> suspendQueue=new ArrayList<Process>();
	private PriorityQueue<Process> priorityQueue;
}
